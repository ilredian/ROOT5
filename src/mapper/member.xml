<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="DAO.MemberDAO">
	<select id="getMember" parameterType="String" resultType="DTO.MemberDTO">
		SELECT * FROM MEMBER WHERE email= #{EMAIL}
	</select> 	

	<insert id="insert" parameterType="DTO.MemberDTO">
		INSERT INTO memberdb(email, password, name, phone, regdate, regpwd, photo, typeno) 
		VALUES(#{email}, #{password}, #{name}, #{phone},SYSDATE(),SYSDATE(),#{photo},0)
	</insert>
</mapper>