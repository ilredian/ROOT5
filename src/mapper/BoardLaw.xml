<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="DAO.BoardLawDAO">
	<select id="getNotices" resultType="DTO.BoardLawDTO">
		SELECT *
		FROM boardlaw
		WHERE ${param2} LIKE '%${param3}%' 
		ORDER BY regdate DESC, boardno DESC
		LIMIT ${param1}, ${param4}
	</select>

	<select id="getCount" resultType="Integer">
		SELECT COUNT(*) CNT
		FROM BOARDLAW
		WHERE ${param1} LIKE '%${param2}%'
	</select>
	
	<select id="getNotice" resultType="DTO.BoardLawDTO">
		SELECT * FROM boardlaw WHERE boardno=#{boardno}

	</select>

	<delete id="delete">
		DELETE FROM boardlaw WHERE boardno=#{boardno}
	</delete>
	
	<update id="update">
		UPDATE boardlaw
		SET 
		TITLE=#{title},
		CONTENT=#{content} 
		WHERE boardno=#{boardno}
	</update>
	
	<insert id="insert" parameterType="DTO.BoardLawDTO">
		<selectKey order="BEFORE" keyProperty="boardno" resultType="String">
			SELECT NVL(MAX(TO_NUMBER(boardno)),0)+1 FROM boardfree
		</selectKey>
		INSERT INTO boardlaw(boardno, TITLE, CONTENT, name, REGDATE,COUNTNO)
		VALUES( #{boardno}, #{title}, #{content}, #{name}, SYSDATE, 0)
	</insert>
</mapper>
