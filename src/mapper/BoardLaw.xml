<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="DAO.BoardLawDAO">
	<select id="getNotices" resultType="DTO.BoardLawDTO">
		SELECT *
		FROM boardlaw
		WHERE ${param2} LIKE '%${param3}%' 
		ORDER BY regdate DESC, boardno DESC
		LIMIT ${param1}, ${param4}
	</select>

	<select id="getCount" resultType="Integer">
		SELECT COUNT(*) CNT
		FROM boardlaw
		WHERE ${param1} LIKE '%${param2}%'
	</select>
	
	<select id="getNotice" parameterType="Integer" resultType="DTO.BoardLawDTO">
		SELECT * FROM boardlaw WHERE boardno=#{boardno}
	</select>
	
	<!--   public BoardLawDTO getNotice(int boardno) throws ClassNotFoundException, SQLException; -->
	<select id="preView" parameterType="Integer" resultType="DTO.BoardLawDTO">
		select * from boardlaw where boardno = #{boardno}
	</select>
	
	<!--  public String preView(int boardno) throws DataAccessException {
	   -->

	<delete id="delete">
		DELETE FROM boardlaw WHERE boardno=#{boardno}
	</delete>
	
	<update id="update">
		UPDATE boardlaw
		SET 
		TITLE=#{title},
		message=#{message},
		 tel=#{tel},
		 phone=#{phone},
		 career=#{career},
		 company=#{company},
		 	edu=#{edu},
		 	place=#{place}
		WHERE boardno=#{boardno}
	</update>
	
	<insert id="insert" parameterType="DTO.BoardLawDTO">
		INSERT INTO boardlaw(TITLE, CONTENT, name, REGDATE,COUNTNO,memberno,career,company,message,phone,tel,edu,fee,place)
		VALUES( #{title}, '글내용없음', #{name}, SYSDATE(), 0,#{memberno},#{career},#{company},#{message},#{phone},#{tel},#{edu},#{fee},#{place})
	</insert>
</mapper>
